import { Button, Container } from "@mui/material";
import { styled } from "@mui/system";

export const StartButton = styled(Button)`
  margin-top: auto;
  font-size: 35px;
`;

export const StartButtonContainer = styled(Container)`
  text-align: center;
  position: relative;
  top: 30vh;
`;
