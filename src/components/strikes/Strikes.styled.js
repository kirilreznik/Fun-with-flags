import { Box, Stack } from "@mui/material";
import { styled } from "@mui/system";

export const StrikesBox = styled(Box)({
  position: "relative",
  left: "85px",
  bottom: "0.01px",
  display: "block",
});

export const StrikeStack = styled(Stack)({});
